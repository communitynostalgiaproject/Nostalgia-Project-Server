import { Request, Response, NextFunction } from "express";
import { NotFoundError } from "../utils/customErrors";

exports.createExperience = (req: Request, res: Response, next: NextFunction) => {
  // To do
  res.send("Route not yet implemented.");
};

exports.getAllExperienceLocations = (req: Request, res: Response, next: NextFunction) => {
  // To do
  res.send("Route not yet implemented.");
};

exports.searchLocationsByGeometry = (req: Request, res: Response, next: NextFunction) => {
  // To do
  res.send("Route not yet implemented.");
};

exports.searchByGeometry = (req: Request, res: Response, next: NextFunction) => {
  // To do
  res.send("Route not yet implemented.");
};

exports.getExperienceById = (req: Request, res: Response, next: NextFunction) => {
  // To do
  res.send("Route not yet implemented.");
};

exports.editExperience = (req: Request, res: Response, next: NextFunction) => {
  // To do
  res.send("Route not yet implemented.");
};

exports.deleteExperience = (req: Request, res: Response, next: NextFunction) => {
  // To do
  res.send("Route not yet implemented.");
};

export default exports;